{% extends "AppAdminBundle::index.html.twig" %}

{% block title %} Profils - New {% endblock %}

{% block body %}
		<div class="container col-lg-10 col-lg-offset-1 content_bo">

			<div class="row">
				<h1 class="text-center title_bo">Profils <i class="fa fa-chevron-right fa-1"></i> New </h1>
			</div>
			
			<div class="well">
				{{ form_start(form, {'attr' : { 'class' : 'form-horizontal'}} ) }}
				{{ form_errors(form) }}

				<div class="form-group">
					{{ form_label(form.title, 'Nom du profil', {'label_attr': { 'class' : 'col-sm-2 control-label' }} ) }}
					{{ form_errors(form.title) }}
					<div class="col-sm-8">
						{{ form_widget(form.title, { 'attr' : {'class' : 'form-control'}} ) }}
					</div>
				</div>
				<div class="save">
					{{ form_widget(form.save, {'attr': {'class': 'btn btn-primary save'}}) }}
				</div>

				{{ form_rest(form) }}
  				{{ form_end(form) }}
			</div>
		</div>
{% endblock %}