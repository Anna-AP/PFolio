{% extends "AppAdminBundle::index.html.twig" %}

{% block title %} Profils {% endblock %}

{% block body %}
		<div class="container col-lg-10 col-lg-offset-1 content_bo">

			<div class="row">
				<h1 class="text-center title_bo">Profils</h1>
			</div>
			
			<div class="row">
				<a href="{{ path('app_admin_profils_new') }}" class="btn create pull-right" role="button">Ajouter un profil</a>
			</div>
			
	        <div class="table-responsive table-challenge">
	            <table class="table table-striped table-hover table-bordered records_list">
	                <thead>
	                <tr>
	                	<th scole="col">Id</th>
	                    <th scope="col">Title</th>
	                    <th scope="col"></th>
	                </tr>
	                </thead>
	                <tbody>
		                {% for prof in profils %}
		                    <tr id="profil{{ prof.id }}">
		                    	<td style="width: 10%;">{{ prof.id }}</td>
		                        <td style="width: 80%;">{{ prof.title }}</td>
		                        <td style="width: 10%;text-align:center;">
		                            <div class="btn-challenge">
	                                    <a class="btn btn-default btn-sm" href=" {{ path('app_admin_profils_edit', {id: prof.id}) }}" alt="{{ 'Modifier'|trans({}, 'admin') }}">
	                                        <i class="fa fa-pencil-square-o fa-lg" aria-hidden="true"></i>
	                                    </a>
	                                    <a class="btn btn-default btn-sm" onclick="return confirm('Êtes vous sur de vouloir supprimer ce profil?')" href="{{ path('app_admin_profils_delete', {id: prof.id}) }}" alt="{{ 'Supprimer'|trans({}, 'admin') }}">
	                                       	<i class="fa fa-times fa-lg" aria-hidden="true"></i>
	                                    </a>	                                
		                            </div>
		                        </td>
		                    </tr>
	                	{% endfor %}
	                </tbody>
	            </table>
	        </div>
        {# {{ knp_pagination_render(paginator) }} #}
		</div>
{% endblock %}